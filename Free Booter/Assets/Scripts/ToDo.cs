using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToDo : MonoBehaviour
{
    /* TODO:
    FIX ENEMY HEALTH BARS
    FIX INVENTORY UI
    START WEATHER CONTROLLER
        CREATE WEATHER OBJECT
        CREATE WEATHER AREA OBJECT
        HAVE WEATHER AREAS CHANGE ANIMATIONS AND PHYSICS TO MATCH WEATHER
        RANDOMIZE WEATHER IN WEATHER AREAS
    QUICK ACCESS BARS
        ADD QUICK ACCESS TOOLBAR UI
        ADD HOTKEYS
        ADD ITEM EQUIPPING
    */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
